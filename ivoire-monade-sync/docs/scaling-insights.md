# Scaling Insights — IVOIRE MONADE curated fallbacks
Use these when web search returns nothing or is unavailable.

Hermes scaling principle: delegate reasoning, keep orchestration local, emit only actionable signal.
Claude scaling principle: constrain context, separate planner from executor, use tool-first prompts.
OBM automation rule: every alert must trigger exactly one owner action; no orphan notifications.
Cron rule: max 3-line output, degraded-safe, repeat only when state changes.
